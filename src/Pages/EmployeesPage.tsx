import { FunctionComponent } from 'react';
import ReactDOM from 'react-dom';
import '../i18n/config';

const EmployeesPage: FunctionComponent = () => {
  return (<></>);
};

ReactDOM.render(<EmployeesPage />, document.getElementById('reactContainer'));
